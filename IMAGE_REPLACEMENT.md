# REVIA LP 写真・画像の差し替え手順

このLPでは、写真を同じファイル名で上書きするだけで画像を差し替えられるようにしています。
HTMLやCSSのコード修正は不要です。

## 1. 新しい写真をどこに保存するか

画像は以下のフォルダに保存します。

```text
outputs/revia-site/images/
```

Vercelで公開する場合も、この `images` フォルダごとアップロード・管理します。

## 2. ファイル名をどうするか

差し替えたい画像に合わせて、必ず以下のファイル名にしてください。

```text
代表者写真: images/profile.jpg
YouTube撮影風景写真: images/youtube-shooting.jpg
REVIイラスト: images/revi-illustration.png
```

ポイント:
- 代表者写真は `profile.jpg` という名前にします。
- YouTube撮影風景は `youtube-shooting.jpg` という名前にします。
- REVIイラストは `revi-illustration.png` という名前にします。
- ファイル名の大文字・小文字も一致させてください。

## 3. 画像の推奨サイズ

読み込み速度を落とさないため、画像は大きすぎないサイズにしてください。

```text
代表者写真: 横 800px 前後
YouTube撮影風景写真: 横 1200px 前後
REVIイラスト: 横 1200px 前後
```

スマホ表示を考えると、1枚あたり 300KB 以下を目安にすると安心です。

## 4. Vercel公開後の更新方法

Vercelで公開している場合、以下のどちらかで更新できます。

### GitHub連携している場合

1. `outputs/revia-site/images/` の画像を同じファイル名で上書きします。
2. 変更をGitHubへ反映します。
3. Vercelが自動で再デプロイします。

### Vercelへ手動アップロードしている場合

1. `outputs/revia-site/images/` の画像を同じファイル名で上書きします。
2. Vercelのプロジェクトへ、更新後のサイト一式を再アップロードします。
3. 新しいデプロイが完了するまで待ちます。

## 5. 再デプロイの方法

GitHub連携の場合は、画像を上書きして反映すると自動で再デプロイされます。

手動の場合は、Vercelの管理画面から新しいデプロイを作成してください。

## 6. 写真変更後の確認方法

1. 公開URLを開きます。
2. 代表紹介セクションの代表者写真が変わっているか確認します。
3. YouTube紹介セクションの撮影風景写真が変わっているか確認します。
4. スマホ幅でも写真が大きすぎないか確認します。
5. 古い画像が表示される場合は、ブラウザを再読み込みしてください。

## 7. 注意点

- 画像を差し替えるときは、同じファイル名で上書きしてください。
- ファイル名を変えるとLPに反映されません。
- 写真が横向き・縦向きで大きく変わる場合は、表示の見え方だけ確認してください。
- プロカメラマン写真に差し替える場合も、最終的なファイル名は `profile.jpg` と `youtube-shooting.jpg` にしてください。
